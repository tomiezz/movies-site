export enum API_PATH {
  GET_PRICE_TABLE = "/prices",
}

export enum APP_ROUTES {
  LOGIN = "/login",
  SIGNUP = "/signup",
  RESULT = "/result",
  DETAIL = "/detail",
  PROFILE = "/profile",
  NOT_FOUND = "/404",
  ERROR = "/500",
  HOME = "/",
}
