export enum API_PATH {
  GET_PRICE_TABLE = "/prices",
}

export enum APP_ROUTES {
  LOGIN = "/login",
  SIGNUP = "/signup",
  RESULT = "/result",
  DETAIL = "/detail",
  PROFILE = "/profile",
  NOT_FOUND = "/404",
  ERROR = "/500",
  HOME = "/",
}

export enum API_PATH {
  LOGIN = "/api/login",
  SIGNUP = "/api/signup",
  LOGOUT = "/api/logout",
}

export enum SERVER_STATUS_CODE {
  NOT_FOUND = 404,
  SERVER_ERROR = 500,
  SUCCESS = 200,
  UNAUTH = 401,
  METHOD_NOT_ALLOWED = 405,
}
